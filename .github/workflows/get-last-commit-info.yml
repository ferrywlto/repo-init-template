on:
  workflow_dispatch:

jobs:
  get-git-info:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Git
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Get commit info
        id: get-commit-info
        run: |
          COMMIT_INFO=$(git log -1 --pretty=format:'%h %ae' $GITHUB_SHA)
          COMMIT_SHA=$(echo $COMMIT_INFO | awk '{print $1}')
          COMMIT_EMAIL=$(echo $COMMIT_INFO | awk '{print $2}')
          echo "commit-sha=${COMMIT_SHA}" >> $GITHUB_OUTPUT
          echo "commit-email=${COMMIT_EMAIL}" >> $GITHUB_OUTPUT
        env:
          COMMIT_SHA: $COMMIT_SHA
          COMMIT_EMAIL: $COMMIT_EMAIL

      - name: Print
        run: |
          echo "Commit ${{ steps.get-commit-info.commit-sha }} is from ${{ steps.get-commit-info.commit-email }}"
